# Environment Configuration for bdcinemas Web Platform
VITE_BASE_PATH=/BDcinemas/

# Optional Authorized Movie Metadata Provider & Backend Gateway
# When deploying in production, secrets can be maintained on a backend proxy:
# VITE_METADATA_PROXY_URL=https://your-api.domain.com/metadata
# Optional direct provider keys for authorized movie metadata:
# VITE_OMDB_API_KEY=
# VITE_TMDB_API_KEY=
